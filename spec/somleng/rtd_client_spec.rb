RSpec.describe Somleng::RtdClient do
  it "has a version number" do
    expect(Somleng::RtdClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
